# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Miscellaneous
/static/

# claude-mem regenerates these context stub files without a trailing
# newline on every touch of their directory, which repeatedly broke the
# prettier gate in CI for content nobody hand-edits.
**/CLAUDE.md
